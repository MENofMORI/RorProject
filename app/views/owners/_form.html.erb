<%= form_with(model: owner, local: true) do |form| %>
  <% if owner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(owner.errors.count, "error") %> prohibited this owner from being saved:</h2>

      <ul>
        <% owner.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="jumbotron col-md-12">
    <div class="field card p-2 w-75">
      <div class="field">
        <%= form.label :name %>
        <%= form.text_field :name, class: 'w-100'%>
      </div>

      <div class="field">
        <%= form.label :sname %>
        <%= form.text_field :sname, class: 'w-100'%>
      </div>

      <div class="field">
        <%= form.label :pesel %>
        <%= form.text_field :pesel, class: 'w-100'%>
      </div>

      <div class="field">
        <%= form.label :email %>
        <%= form.text_field :email, class: 'w-100'%>
      </div>

      <div class="field">
        <%= form.label :telefon %>
        <%= form.text_field :telefon, class: 'w-100'%>
      </div>
    </div>

    <div class="actions">
      <%= form.submit class: 'btn btn-info btn-sm w-75'%>
    </div>
  </div>
<% end %>
